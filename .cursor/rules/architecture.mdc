---
description: Core project architecture and layer boundaries
alwaysApply: true
---

# Architecture

`AGENTS.md` is the source of truth for this repo — read it before substantive changes.

Quick guardrails (details and examples in `AGENTS.md`):

- Layers: `api` → `soccar` → `db` → use cases → `commands`
- `data.soccar_api()` at call sites; `Season::default_for_guild()` in gameplay commands
